## Local-development defaults only.
##
## New queues declared without an explicit type use quorum queues. Business
## topology, retry, DLQ, and Streams are intentionally not configured yet.
default_queue_type = quorum
