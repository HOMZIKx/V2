{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web",
  "projectType": "application",
  "tags": ["type:app", "scope:web"],
  "targets": {
    "serve": { "command": "corepack pnpm --dir apps/web dev" },
    "build": {
      "command": "corepack pnpm --dir apps/web build",
      "outputs": ["{projectRoot}/.next"]
    },
    "lint": {
      "command": "corepack pnpm exec eslint apps/web/app apps/web/src apps/web/middleware.ts"
    },
    "typecheck": { "command": "corepack pnpm exec tsc -p apps/web/tsconfig.json --noEmit" },
    "test": { "command": "corepack pnpm exec vitest run --config apps/web/vitest.config.ts" },
    "e2e": {
      "command": "corepack pnpm exec playwright test --config apps/web/playwright.config.ts"
    }
  }
}
