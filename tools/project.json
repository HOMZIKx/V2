{
  "name": "tools",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "tools",
  "projectType": "library",
  "tags": ["type:tooling"],
  "targets": {
    "test": {
      "command": "corepack pnpm exec vitest run --config tools/vitest.config.ts"
    },
    "test-infra": {
      "command": "corepack pnpm exec vitest run --config tools/infra/vitest.config.ts"
    }
  }
}
