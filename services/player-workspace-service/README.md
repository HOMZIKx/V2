# player-workspace-service

NestJS 11 and Fastify service scaffold with Domain, Application, Infrastructure, and Interface layers.

Default port: `4500`. Data ownership: `database`.
The default host is `127.0.0.1`. Set `PLAYER_WORKSPACE_SERVICE_HOST=0.0.0.0` only in a container or deployment that explicitly requires external binding.
