import { describe, expect, it } from 'vitest';

describe('HealthController contract', () => {
  it('exposes live/ready routes via AppModule (smoke placeholder)', () => {
    expect(true).toBe(true);
  });
});
