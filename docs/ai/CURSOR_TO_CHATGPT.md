# Cursor → Owner

## Status

Dwa oddzielne systemy mapowe dopięte wg Twojego opisu:

1. **Timery** (`/timers`, DEC-066) — zbicie → timer → okno → pinezka ostatniego zbicia
2. **Party** (`/maps`, DEC-067) — drużyna → pinezka skauta ~10 min → odklik →
   licznik zbić w sesji

## Immediate next (D-061 SoT)

Stabilizacja first-player + create/edit setów/itemów/progression; bez API/Discord
prod dopóki nie poprosisz.

## Marker

`READY_FOR_OWNER_REVIEW` + `DEC-066` + `DEC-067`
